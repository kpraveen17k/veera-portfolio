================================================================================
VEERA OFFICIAL - MASTER UI DESIGN SYSTEM & CSS GUIDE
================================================================================

1. BRAND COLOR PALETTE & CSS VARIABLES
--------------------------------------------------------------------------------
:root {
  /* Core Brand Colors */
  --primary-purple: #8b5cf6;
  --accent-pink: #ec4899;
  --accent-blue: #0284c7;
  --accent-green: #10b981;
  --accent-orange: #f59e0b;
  --accent-cyan: #06b6d4;
  --accent-rose: #e11d48;

  /* Signature Gradients */
  --accent-gradient: linear-gradient(135deg, #3b82f6 0%, #a855f7 50%, #ec4899 100%);
  --hero-gradient: linear-gradient(135deg, #ec4899 0%, #a855f7 50%, #6366f1 100%);
  --btn-gradient: linear-gradient(135deg, #a855f7 0%, #6366f1 100%);
  --sidebar-active-gradient: linear-gradient(90deg, #ec4899 0%, #f43f5e 100%);
  --dark-panel-gradient: linear-gradient(135deg, #1e1b4b 0%, #312e81 100%);

  /* Backgrounds & Text */
  --card-white: #ffffff;
  --page-bg: #f8fafc;
  --body-bg-alt: #f1f5f9;
  --text-dark: #1e293b;
  --text-dark-deep: #0f172a;
  --text-muted: #64748b;
  --border-subtle: #e2e8f0;
  --border-light: #f1f5f9;

  /* Radii & Shadows */
  --radius-xl: 20px;
  --radius-lg: 14px;
  --radius-md: 10px;
  --shadow-card: 0 10px 30px rgba(0, 0, 0, 0.03), 0 1px 3px rgba(0, 0, 0, 0.02);
  --glow-purple: 0 8px 20px rgba(168, 85, 247, 0.35);
  --glow-pink: 0 8px 20px rgba(236, 72, 153, 0.25);
}

--------------------------------------------------------------------------------
2. BADGE & METRIC ICON PALETTES
--------------------------------------------------------------------------------
Purple : bg: #ede9fe; color: #8b5cf6;
Pink   : bg: #fce7f3; color: #ec4899;
Blue   : bg: #e0f2fe; color: #0284c7;
Green  : bg: #dcfce7; color: #10b981;
Orange : bg: #fef3c7; color: #f59e0b;
Red    : bg: #fee2e2; color: #ef4444;

--------------------------------------------------------------------------------
3. ANIMATIONS, TRANSITIONS & HOVER EFFECTS
--------------------------------------------------------------------------------
- Standard Transition : transition: all 0.25s ease;
- Hover Lift (Buttons): transform: translateY(-2px);
- Hover Lift (Cards)  : transform: translateY(-4px);
- Image Hover Zoom    : transform: scale(1.05); transition: transform 0.4s ease;
- Modal Keyframes:
  @keyframes modalPop {
    from { opacity: 0; transform: scale(0.95); }
    to { opacity: 1; transform: scale(1); }
  }
- Tab Pane Transition:
  .tab-pane {
    display: none;
    opacity: 0;
    transform: translateY(6px);
    transition: opacity 0.3s ease, transform 0.3s ease;
  }
  .tab-pane.active {
    display: block;
    opacity: 1;
    transform: translateY(0);
  }

--------------------------------------------------------------------------------
4. CLEAN INPUT FOCUS FIX (NO RED WARNING/ERROR OUTLINE)
--------------------------------------------------------------------------------
.input-box input {
  width: 100%;
  padding: 12px 16px;
  border-radius: 12px;
  border: 1.5px solid #e2e8f0;
  background: #ffffff;
  color: #1e293b;
  font-size: 13.5px;
  outline: none !important;
  box-shadow: none !important;
  pointer-events: auto !important;
  user-select: text !important;
}

/* Prevent default browser red invalid outlines */
.input-box input:invalid,
.input-box input:user-invalid {
  border-color: #e2e8f0 !important;
  box-shadow: none !important;
}

/* Soft Purple Glow Focus */
.input-box input:focus {
  border-color: #8b5cf6 !important;
  box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.15) !important;
}

--------------------------------------------------------------------------------
5. REUSABLE BASE CSS TEMPLATE FOR NEW PAGES
--------------------------------------------------------------------------------
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Plus Jakarta Sans', sans-serif;
}

body {
  background: #f8fafc;
  color: #1e293b;
  min-height: 100vh;
}

.app-layout {
  display: flex;
  min-height: 100vh;
}

/* Sidebar */
.sidebar {
  width: 250px;
  background: #ffffff;
  border-right: 1px solid #e2e8f0;
  padding: 24px 16px;
  display: flex;
  flex-direction: column;
  flex-shrink: 0;
}

.crown-emblem {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  background: linear-gradient(135deg, #3b82f6 0%, #a855f7 50%, #ec4899 100%);
  display: grid;
  place-items: center;
  color: #ffffff;
  font-size: 20px;
  box-shadow: 0 8px 20px rgba(168, 85, 247, 0.35);
}

.nav-item {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 10px 14px;
  border-radius: 14px;
  font-size: 13.5px;
  font-weight: 600;
  color: #64748b;
  text-decoration: none;
  transition: all 0.25s ease;
}

.nav-item:hover {
  background: #f8fafc;
  color: #1e293b;
}

.nav-item.active {
  background: linear-gradient(135deg, #3b82f6 0%, #a855f7 50%, #ec4899 100%);
  color: #ffffff;
  box-shadow: 0 8px 20px rgba(236, 72, 153, 0.25);
}

.main-content {
  flex: 1;
  padding: 24px 30px 40px;
  display: flex;
  flex-direction: column;
  gap: 20px;
  overflow-y: auto;
}

/* Mobile Responsiveness */
.mobile-bottom-nav { display: none; }

@media (max-width: 900px) {
  .sidebar { display: none; }
  .main-content { padding: 16px 16px 85px; }

  .mobile-bottom-nav {
    display: flex;
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    background: #ffffff;
    border-top: 1px solid #f1f5f9;
    padding: 10px 16px 14px;
    justify-content: space-around;
    z-index: 100;
  }

  .b-nav-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    font-size: 10px;
    font-weight: 600;
    color: #64748b;
    text-decoration: none;
  }

  .b-nav-item.active {
    color: #8b5cf6;
  }
}
================================================================================